# ReadMe for the API endpoint

### This page is for my project partner to understand the API endpoint, how to use it and also when to use it.


The database will first be made locally with regular SQL.

Once the database is initialised and fully created locally, we can move the logic [refactor] it to use the API endpoint that will be created
api endpoint will be used here to fetch the data from the database and then display it on the frontend.
Same logic applies so minimal change in the code, for now we'll have the api endpoint point to the local database.
Therefore, still a need for the API.js file to be created and used in the frontend to fetch the data from the database and display it on the frontend.
To refactor its just going to be a simple replace.

### API Endpoint
```  (Endpoint Link/URL once it's made.)  ```

### How to use it
[Will be populated once there is data for me to provide here.]